let dishes = ["Chicken", "Mutton", "beef"];

/// it returns the index 
for (favoriteDishes in dishes){
    console.log(favoriteDishes);
}
/// to return the values in the array 
for (favoriteDishes in dishes){
    console.log(dishes[favoriteDishes]);
}